name: newsapp_mobile
description: NewsApp mobile (Flutter + Riverpod + GoRouter + Dio)
environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.11

flutter:
  uses-material-design: true
  assets:
    - lib/assets/images/
    - lib/assets/icons/
